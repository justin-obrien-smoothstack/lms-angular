import { PropertySwapPipe } from './property-swap.pipe';

describe('PropertySwapPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertySwapPipe();
    expect(pipe).toBeTruthy();
  });
});
